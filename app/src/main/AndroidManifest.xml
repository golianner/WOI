<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wto.com.wto">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DeveloperRegisterActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".EmailVerificationActivity" />
        <activity android:name=".MyInterestActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".InboxActivity" />
        <activity android:name=".DetailInboxActivity" />
        <activity android:name=".OverviewStatistikActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".UploadPromotionActivity" />
        <activity android:name=".TutorialActivity"></activity>
    </application>

</manifest>